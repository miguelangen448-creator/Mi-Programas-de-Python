# una condicion en python se escribe:
#"if {variable}:
#	codigo para ejecutar"
# para checar si es verdadero un boolean (True o False)
# Para comparar es:
#"if {variable} {==, <, <=, > o >=} {str, int o float}:
#	codigo para ejecutar"

# else es para ejecutar un codigo si la condicion no se cumple:
#"if {condicion}:
#	codigo para ejecutar
#else:
#	codigo para ejecutar"

#elif es para ejecutar un codigo con una condicion si la condicion previa no se cumple:
#"if {condicion}:
#	codigo para ejecutar
#elif {condicion}:
#	codigo para ejecutar















































print("lee el codigo :v")